
  # Smart Medicine Reminder UI

  This is a code bundle for Smart Medicine Reminder UI. The original project is available at https://www.figma.com/design/RawcWspnsvX6EYt6VkpXhP/Smart-Medicine-Reminder-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  